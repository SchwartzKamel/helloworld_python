version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    command: gunicorn --bind 0.0.0.0:8000 app.main:app

  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"